### rebal_world_ep3 —— 主线冲分候选(2026-07-03,平台上传名=本名,全链统一)
### = seed_ep3(0.8931)配方原封不动,数据换 data_rebal_world:
###   数据(29019条, build_rebalanced.py): rec域重平衡(video 16494→6000, ad/living/prod 各上采样至4000)+ 8%中文通识(world_zh 2824条)
###   训练: lr2e-5(官方全参推荐值,平台指南确认)/ 3 epoch / batch4(bs1×accum4)/ cutoff32768(指南推荐值)
### 对照: seed_ep3(纯种子/同超参)=0.8931 → 变量=数据(重平衡+通识,组合实验已在log标注)
### 目标: 保物料0.245 + ad靠upsample回血 + 通识加分 → 0.91+
### 出分后必查: 物料是否从0.2453回落(ad上采样动的是video_ad共享子空间,recipe2翻车同源风险)
### save_total_limit 3 + save_strategy epoch → 顺带免费拿到 ep1/ep2 中间ckpt(2ep是从未测过的跷跷板拐点)
model_name_or_path: /lustre/prod_glm_volumes/volume-20260201002229-o7c51/llmrec_2026/models/OneReason-0.8B-pretrain-competition
trust_remote_code: true
flash_attn: fa2

stage: sft
do_train: true
finetuning_type: full
enable_liger_kernel: true

dataset: data_rebal_world
dataset_dir: /lustre/prod_glm_volumes/volume-20260201002229-o7c51/ai_runtime/llmrec_2026/LLaMA-Factory/data
template: qwen3_nothink
cutoff_len: 32768
packing: true
neat_packing: true
overwrite_cache: true
preprocessing_num_workers: 16
dataloader_num_workers: 8

### 验证集 + token accuracy
val_size: 0.03
eval_strategy: steps
eval_steps: 100
per_device_eval_batch_size: 1
compute_accuracy: true

output_dir: /lustre/prod_glm_volumes/volume-20260201002229-o7c51/llmrec_2026/checkpoints/rebal_world_ep3
logging_steps: 5
save_strategy: epoch
save_total_limit: 3
plot_loss: true
overwrite_output_dir: true
report_to: wandb
run_name: rebal_world_ep3

### train — 官方全参配方(平台指南), 仅 epoch 1→3
per_device_train_batch_size: 1
gradient_accumulation_steps: 4
learning_rate: 2.0e-5
num_train_epochs: 3
lr_scheduler_type: cosine
warmup_ratio: 0.03
weight_decay: 0.0
bf16: true
pure_bf16: true
seed: 19260817
